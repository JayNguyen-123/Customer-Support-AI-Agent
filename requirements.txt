# =====================================================================
# 1. Core Web Framework & Server Layer
# =====================================================================
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
httpx==0.27.0

# =====================================================================
# 2. LangGraph & LangChain Orchestration Core
# =====================================================================
langgraph==0.0.69
langchain-core==0.2.10
langchain-openai==0.1.13
langchain-chroma==0.1.2
langchain-community==0.2.6
# Provides langgraph.checkpoint.sqlite / .aio -- main.py imports
# AsyncSqliteSaver from here for conversation checkpointing. This was
# missing entirely from the original requirements.txt even though main.py
# imported a checkpoint backend package. Verify this version against
# whatever `langgraph` core version you actually resolve/upgrade to --
# checkpoint backends are versioned somewhat independently of langgraph
# core, and 0.0.69 here is quite an old pin.
langgraph-checkpoint-sqlite==1.0.0

# =====================================================================
# 3. Persistent Database & Migration Engine (SQLAlchemy Stack)
# =====================================================================
sqlalchemy==2.0.31
alembic==1.13.2
passlib[bcrypt]==1.7.4
# Pinned explicitly: passlib 1.7.4's bcrypt backend probes `bcrypt.__about__`,
# which bcrypt >=4.1 removed -- installing an unpinned/newer bcrypt alongside
# this passlib version raises AttributeError the first time you hash or
# verify a password (i.e. the moment /api/v1/auth/register or /login runs).
bcrypt==4.0.1
pyjwt==2.9.0

# =====================================================================
# 4. Hybrid Search, Parsing & Vector Utilities (RAG)
# =====================================================================
chromadb==0.5.3
rank-bm25==0.2.2
flashrank==0.2.8
pypdf==4.2.0

# =====================================================================
# 5. Production Observability, Metrics & Distributed Tracing
# =====================================================================
prometheus-client==0.20.0
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0
opentelemetry-exporter-otlp-proto-grpc==1.25.0
